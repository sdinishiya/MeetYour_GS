const USER_TYPES = {
    ADMIN: "admin",
    CLIENT: "client",
    GUEST: "guest"
}

const NOTIFICATION_STATUS = {
    UNREAD: 0,
    READ: 1,
    DELETED: 2
}

module.exports = {USER_TYPES, NOTIFICATION_STATUS}
